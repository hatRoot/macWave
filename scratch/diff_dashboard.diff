diff --git a/dashboard-ods.html b/dashboard-ods.html
index eab9e49..7be3e98 100755
--- a/dashboard-ods.html
+++ b/dashboard-ods.html
@@ -16,15 +16,15 @@
 
  <script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
  <script src="js/supabase-config.js"></script>
- <script src="js/ops-shared.js?v=hotfix-2b"></script>
+ <script src="js/ops-shared.js?v=hotfix-3a"></script>
  <script>
-  (async function requireSupabaseSession() {
+  window.__mwDashboardReady = (async function bootstrapDashboardSession() {
     const client = MacWaveOps.createSupabaseClient();
     const { data } = await client.auth.getSession();
     if (!data?.session) {
       sessionStorage.clear();
       window.location.replace('tecnicos.html');
-      return;
+      return null;
     }
     const authEmail = (data.session.user.email || '').trim().toLowerCase();
     const role = await MacWaveOps.resolveOpsAccess(client, authEmail);
@@ -32,8 +32,11 @@
       await client.auth.signOut();
       sessionStorage.clear();
       window.location.replace('tecnicos.html');
-      return;
+      return null;
     }
+    sessionStorage.setItem('macwave_user', authEmail);
+    sessionStorage.setItem('macwave_role', role);
+    return { client, authEmail, role };
   })();
  </script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/html2canvas/1.4.1/html2canvas.min.js"></script>
@@ -76,6 +79,7 @@
  color: var(--mac-text);
  height: 100vh;
  display: flex;
+ flex-direction: column;
  overflow: hidden;
  }
 
@@ -171,6 +175,7 @@ flex: 1;
  overflow: hidden;
  background: transparent;
  position: relative;
+ min-height: 0;
  }
 
  .content-area {
@@ -374,6 +379,7 @@ flex: 1;
  display: flex;
  flex: 1;
  overflow: hidden;
+ min-height: 0;
  }
 
  /* Sidebar - List View */
@@ -814,9 +820,38 @@ white-space: nowrap;
  }
 
  .mobile-top-nav {
+ display: flex;
+ position: sticky;
+ top: 0;
+ height: 56px;
+ background: rgba(9, 9, 11, 0.98);
+ backdrop-filter: blur(20px);
+ border-bottom: 1px solid var(--mac-border);
+ z-index: 10000;
+ justify-content: flex-start;
+ align-items: center;
+ gap: 2px;
+ padding: 0 8px;
+ flex-shrink: 0;
+ overflow-x: auto;
+ }
+
+ .app-sidebar {
+ display: none !important;
+ }
+
+ body.view-list .app-main {
+ flex-direction: column;
+ }
+
+ .mobile-nav-item.admin-only {
  display: none;
  }
 
+ body.is-admin .mobile-nav-item.admin-only {
+ display: flex;
+ }
+
  /* Progress Overlay */
  #upload-progress-overlay {
  position: fixed;
@@ -1091,6 +1126,18 @@ word-break: break-word;
  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M15 5l-1.41 1.41L15.17 8H4v2h11.17l-1.58 1.59L15 13l4-4-4-4z"/></svg>
  <span>TICKET</span>
  </button>
+ <button class="mobile-nav-item admin-only" id="m-nav-activity" onclick="switchView('activity')">
+ <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/></svg>
+ <span>LOG</span>
+ </button>
+ <button class="mobile-nav-item admin-only" id="m-nav-finance" onclick="switchView('finance')">
+ <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 1v22M17 5H9.5a3.5 3.5 0 000 7h5a3.5 3.5 0 010 7H6"/></svg>
+ <span>$$</span>
+ </button>
+ <button class="mobile-nav-item" id="m-nav-logout" title="Cerrar sesión" style="margin-left: auto; color: var(--mac-red);">
+ <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M9 21H5a2 2 0 01-2-2V5a2 2 0 012-2h4M16 17l5-5-5-5M21 12H9"/></svg>
+ <span>SALIR</span>
+ </button>
  </nav>
 
  <!-- Windows Vista Aero Sidebar Navigation -->
@@ -1167,7 +1214,7 @@ word-break: break-word;
 
  <main class="app-main">
  <!-- List View -->
- <div class="workspace" id="list-view-container" style="display: flex; height: 100%;">
+ <div class="workspace" id="list-view-container" style="display: flex; flex: 1; min-height: 0; height: 100%;">
  <aside class="sidebar">
  <div class="sidebar-header" style="padding: 10px 12px; display: flex; gap: 8px; align-items: center;">
  <div class="search-wrapper" style="flex: 1;">
@@ -1275,7 +1322,7 @@ word-break: break-word;
  style="flex: 1; margin: 0;">
  <button class="nav-item"
  style="background: var(--mac-blue); color: white; width: max-content; margin: 0; padding: 0 30px;"
- onclick="addCatalogItem()">+ Gurdar en Catálogo</button>
+ onclick="addCatalogItem()">+ Guardar en Catálogo</button>
  </div>
  <div class="table-container"
  style="background: #1c1c1e; border: 1px solid rgba(255,255,255,0.05); border-radius: 12px; overflow: hidden;">
@@ -1567,8 +1614,8 @@ word-break: break-word;
 
  <!-- ODS View (Iframe) -->
  <div class="workspace" id="quote-view-container"
- style="display: none; height: 100%; border: none; background: #000; overflow: hidden;">
- <iframe src="ods.html?embed=true" style="width: 100%; height: 100%; border: none;"></iframe>
+ style="display: none; height: 100%; border: none; background: #000; overflow: hidden; flex: 1; min-height: 0;">
+ <iframe id="ods-quote-iframe" src="about:blank" title="Generador ODS PDF" style="width: 100%; height: 100%; border: none; display: block; background: #000;"></iframe>
  </div>
  </main>
 
@@ -1650,22 +1697,74 @@ word-break: break-word;
  const supabaseClient = MacWaveOps.createSupabaseClient();
 
  async function mwPersistOrden(id, patch, auditAction, auditDetails) {
- const o = allODS.find((x) => x.id === id);
- MacWaveOps.enrichOrdenPatch(patch, o?.fecha || patch.fecha);
- const { error, count } = await supabaseClient.from('ordenes_servicio').update(patch).eq('id', id);
- if (!error && auditAction) {
- await MacWaveOps.logAudit(supabaseClient, {
- ods_id: id,
- folio: o?.folio || patch.folio,
- action: auditAction,
- details: auditDetails || {},
- });
- if (currentView === 'activity' && window.MacWaveAuditLog) {
- MacWaveAuditLog.refresh();
- }
- }
- return { error, count };
- }
+  const o = allODS.find((x) => x.id === id);
+
+  if (o && o.isTicketOnly) {
+    // 1. Build the complete new ODS row
+    const newODSRow = {
+      id: o.id, // Keep the same UUID!
+      folio: o.folio,
+      cliente: o.cliente || 'Cliente',
+      proyecto: o.proyecto ? o.proyecto.replace('Ticket: ', '') : 'Reparación de Computadora',
+      tecnico: o.tecnico || 'JOEL DURAN',
+      serie: o.serie || '',
+      fecha: o.fecha || new Date().toLocaleString('es-MX'),
+      monto: o.monto || 0,
+      status: patch.status || o.status || 'Recibido',
+      tracking_token: o.tracking_token || MacWaveOps.generateTrackingToken(),
+      notas: patch.notas || o.notas || '[]',
+    };
+
+    // Enrich with client_timeline
+    MacWaveOps.enrichOrdenPatch(newODSRow, newODSRow.fecha);
+
+    // 2. Insert into Supabase
+    const { error: insertErr } = await supabaseClient.from('ordenes_servicio').insert([newODSRow]);
+    if (insertErr) {
+      return { error: insertErr, count: 0 };
+    }
+
+    // 3. Update the ticket in tickets_taller to set its ods_id = o.id
+    const { error: ticketErr } = await supabaseClient
+      .from('tickets_taller')
+      .update({ ods_id: o.id })
+      .eq('id', o.id);
+
+    if (ticketErr) {
+      console.warn("Failed to link ticket to ODS:", ticketErr.message);
+    }
+
+    // Remove the isTicketOnly flag from our local copy so future updates run as updates!
+    delete o.isTicketOnly;
+    // Update local fields to match new status/patch
+    Object.assign(o, patch);
+
+    if (auditAction) {
+      await MacWaveOps.logAudit(supabaseClient, {
+        ods_id: id,
+        folio: o.folio,
+        action: auditAction,
+        details: auditDetails || {},
+      });
+    }
+    return { error: null, count: 1 };
+  }
+
+  MacWaveOps.enrichOrdenPatch(patch, o?.fecha || patch.fecha);
+  const { error, count } = await supabaseClient.from('ordenes_servicio').update(patch).eq('id', id);
+  if (!error && auditAction) {
+  await MacWaveOps.logAudit(supabaseClient, {
+  ods_id: id,
+  folio: o?.folio || patch.folio,
+  action: auditAction,
+  details: auditDetails || {},
+  });
+  if (currentView === 'activity' && window.MacWaveAuditLog) {
+  MacWaveAuditLog.refresh();
+  }
+  }
+  return { error, count };
+  }
 
  const STATUS_CONFIG = {
  'Recibido': { color: '#FFD60A' }, // Yellow
@@ -1688,15 +1787,20 @@ word-break: break-word;
  let currentView = 'list'; // 'list' or 'kanban'
 
  const userRole = sessionStorage.getItem('macwave_user');
- const isAdmin = sessionStorage.getItem('macwave_role') === 'admin';
-
- // Init admin UI
- window.addEventListener('DOMContentLoaded', () => {
- if (isAdmin) {
- if (document.getElementById('nav-finance')) document.getElementById('nav-finance').style.display = 'flex';
- if (document.getElementById('nav-settings')) document.getElementById('nav-settings').style.display = 'flex';
+ function isAdminUser() {
+ return sessionStorage.getItem('macwave_role') === 'admin';
  }
+
+ function applyAdminUI() {
+ if (!isAdminUser()) return;
+ document.body.classList.add('is-admin');
+ ['nav-finance', 'nav-settings'].forEach((id) => {
+ const el = document.getElementById(id);
+ if (el) el.style.display = 'flex';
  });
+ }
+
+ // Init admin UI after session bootstrap (see DOMContentLoaded)
 
  function getStatusColor(status) {
  return STATUS_CONFIG[status] ? STATUS_CONFIG[status].color : '#8E8E93';
@@ -1746,6 +1850,8 @@ word-break: break-word;
  const deletedFolios = JSON.parse(localStorage.getItem('t800_deleted_folios') || '[]');
  allTickets = (ticketRes.data || []).filter(t => !deletedFolios.includes(t.folio));
 
+
+
  // Refresh Current View
  if (currentView === 'list') {
  renderSidebar();
@@ -1932,69 +2038,11 @@ word-break: break-word;
  };
 
  function downloadICS() {
- const dateInput = document.getElementById('t-unified-date').value;
- const timeInput = document.getElementById('t-unified-time').value;
- const name = document.getElementById('t-unified-name').value || 'Cliente';
- const folio = document.getElementById('t-unified-folio').innerText;
-
- if (!name) {
- alert("Por favor, ingresa el nombre del cliente.");
- return;
- }
- if (!dateInput || !timeInput) {
- alert("Selecciona fecha y hora primero.");
- return;
- }
-
- const startDate = new Date(`${dateInput}T${timeInput}`);
- const endDate = new Date(startDate.getTime() + 60 * 60 * 1000); // 1 hour
-
- const formatDate = (date) => date.toISOString().replace(/-|:|\.\d+/g, '');
-
- const icsContent = `BEGIN:VCALENDAR\nVERSION:2.0\nBEGIN:VEVENT\nSUMMARY:Cita MacWave - ${folio} (${name})\nDTSTART:${formatDate(startDate)}\nDTEND:${formatDate(endDate)}\nDESCRIPTION:Cita de servicio técnico agendada. Folio: ${folio}.\nLOCATION:MacWave, Av. México Coyoacán 276, Sta Cruz Atoyac, CDMX\nEND:VEVENT\nEND:VCALENDAR`;
-
- const blob = new Blob([icsContent], { type: 'text/calendar;charset=utf-8' });
- const link = document.createElement('a');
- link.href = window.URL.createObjectURL(blob);
- link.setAttribute('download', `Cita_${folio}.ics`);
- document.body.appendChild(link);
- link.click();
- document.body.removeChild(link);
+   return window.downloadICS();
  }
 
  function shareEvent() {
- const dateInput = document.getElementById('t-unified-date').value;
- const timeInput = document.getElementById('t-unified-time').value;
- const name = document.getElementById('t-unified-name').value || 'Cliente';
- const folio = document.getElementById('t-unified-folio').innerText;
- const source = document.getElementById('t-unified-source').value;
- const phone = document.getElementById('t-unified-phone').value;
-
- if (!name) {
- alert("Por favor, ingresa el nombre del cliente.");
- return;
- }
- if (!dateInput || !timeInput) {
- alert("Selecciona fecha y hora primero.");
- return;
- }
- 
- const combinedDate = `${dateInput}T${timeInput}`;
- const gCalLink = generateGoogleCalendarLink(folio, name, source, phone, combinedDate);
-
- const shareData = {
- title: `Cita MacWave - ${folio}`,
- text: `🎟️ TICKET CREADO: ${folio}\nCliente: ${name}\n\n📍 Ubicación: Av. México Coyoacán 276, CDMX\n📅 Agendar en calendario cruzado:\n`,
- url: gCalLink
- };
-
- if (navigator.share) {
- navigator.share(shareData).catch(console.error);
- } else {
- navigator.clipboard.writeText(`${shareData.text} ${shareData.url}`).then(() => {
- alert("Enlace y detalles copiados al portapapeles (Compartir no soportado en este navegador).");
- });
- }
+   return window.shareEvent();
  }
 
  window.sendTicketAction = async function () {
@@ -2169,10 +2217,11 @@ word-break: break-word;
  function switchView(view) {
  console.log("Switching to view:", view);
  currentView = view;
+ document.body.classList.toggle('view-list', view === 'list');
 
  // Role check for restricted views
  if (view === 'finance' || view === 'settings') {
- if (!isAdmin) {
+ if (!isAdminUser()) {
  alert('Acceso restringido: Solo administradores pueden entrar aquí.');
  return;
  }
@@ -2201,19 +2250,32 @@ word-break: break-word;
  const targetId = views[view];
  const target = document.getElementById(targetId);
  if (target) {
- target.style.display = (view === 'list') ? 'flex' : 'block';
- if (view === 'list') target.style.display = 'flex';
- if (view === 'tickets') target.style.display = 'flex'; // Use flex for unified layout
- if (view === 'settings') {
+ if (view === 'list') {
+ target.style.display = 'flex';
+ target.style.flex = '1';
+ target.style.minHeight = '0';
+ } else if (view === 'tickets') {
+ target.style.display = 'flex';
+ } else if (view === 'quote') {
+ target.style.display = 'flex';
+ target.style.flexDirection = 'column';
+ target.style.flex = '1';
+ target.style.minHeight = '0';
+ const iframe = document.getElementById('ods-quote-iframe');
+ if (iframe && !iframe.dataset.loaded) {
+ iframe.dataset.loaded = '1';
+ iframe.src = 'ods.html?embed=true';
+ }
+ } else if (view === 'settings') {
  target.style.display = 'flex';
  renderUsers();
- }
- if (view === 'quote') {
- target.style.display = 'block';
- }
- if (view === 'activity') {
+ } else if (view === 'activity') {
  target.style.display = 'flex';
  target.style.flexDirection = 'column';
+ target.style.flex = '1';
+ target.style.minHeight = '0';
+ } else {
+ target.style.display = 'block';
  }
  }
 
@@ -2371,7 +2433,7 @@ word-break: break-word;
  </td>
  <td style="padding: 12px; color: var(--mac-text-muted); font-size: 11px;">${t.origen || 'N/A'}</td>
  <td style="padding: 12px; text-align: center;">
- ${isAdmin ? `<button onclick="deleteTicketTaller('${t.id}', '${t.folio}')" style="background:transparent; border:none; cursor:pointer; color:var(--mac-red);" title="Eliminar ticket">
+ ${isAdminUser() ? `<button onclick="deleteTicketTaller('${t.id}', '${t.folio}')" style="background:transparent; border:none; cursor:pointer; color:var(--mac-red);" title="Eliminar ticket">
  <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="3 6 5 6 21 6"></polyline><path d="M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6m3 0V4a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2"></path></svg>
  </button>` : ''}
  </td>
@@ -2393,7 +2455,7 @@ word-break: break-word;
  const btn = document.getElementById('btn-bulk-delete');
  const counter = document.getElementById('selected-count');
  
- if (selected.length > 0 && typeof isAdmin !== 'undefined' && isAdmin) {
+ if (selected.length > 0 && isAdminUser()) {
  btn.style.display = 'inline-flex';
  counter.textContent = selected.length;
  } else {
@@ -2751,13 +2813,30 @@ const statuses = [...configuredStatuses, ...dynamicStatuses];
  let targetInitialStatus = 'Recibido';
 
  function openQuickCreate(status = 'Recibido') {
- targetInitialStatus = status;
- document.getElementById('quick-create-modal').classList.add('active');
- // Suggest a folio
- const lastFolio = allODS.length > 0 ? allODS[0].folio : 'MW-1000';
- const num = parseInt(lastFolio.replace(/\D/g, '')) || 1000;
- document.getElementById('new-ods-folio').value = `MW-${num + 1}`;
- }
+    targetInitialStatus = status;
+    document.getElementById('quick-create-modal').classList.add('active');
+    
+    // Suggest a folio
+    let maxNum = 9550; // Baseline: we are at 9550, next should be 9551
+    
+    allODS.forEach(item => {
+      if (item.folio && item.folio.includes('-')) {
+        const parts = item.folio.split('-');
+        const numPart = parseInt(parts[parts.length - 1]);
+        if (!isNaN(numPart) && numPart > maxNum) {
+          maxNum = numPart;
+        }
+      }
+    });
+
+    const nextNum = maxNum + 1;
+    const now = new Date();
+    const yyyy = now.getFullYear();
+    const mm = String(now.getMonth() + 1).padStart(2, '0');
+    const dd = String(now.getDate()).padStart(2, '0');
+    
+    document.getElementById('new-ods-folio').value = `MW-${yyyy}${mm}${dd}-${nextNum}`;
+  }
 
  function closeQuickCreate() {
  document.getElementById('quick-create-modal').classList.remove('active');
@@ -2958,10 +3037,6 @@ filtered.sort((a, b) => {
  });
  }
 
- document.getElementById('search-input').addEventListener('input', (e) => {
- renderSidebar(e.target.value);
- });
-
  function renderDetail(o) {
  currentSelectedId = o.id;
  window.currentSelectedId = o.id;
@@ -3313,21 +3388,27 @@ ${statusOptions}
  };
 
  document.getElementById('btn-delete').onclick = async () => {
- if (!confirm(`¿Eliminar la orden ${o.folio} permanentemente?`)) return;
- try {
- const { error, count } = await supabaseClient.from('ordenes_servicio').delete({ count: 'exact' }).eq('id', o.id);
- if (error) throw error;
- if (count === 0) return alert('No se pudo borrar, revise RLS en Supabase.');
+  if (!confirm(`¿Eliminar la orden ${o.folio} permanentemente?`)) return;
+  try {
+  if (o.isTicketOnly) {
+    const { error, count } = await supabaseClient.from('tickets_taller').delete({ count: 'exact' }).eq('id', o.id);
+    if (error) throw error;
+    allTickets = allTickets.filter(x => x.id !== o.id);
+  } else {
+    const { error, count } = await supabaseClient.from('ordenes_servicio').delete({ count: 'exact' }).eq('id', o.id);
+    if (error) throw error;
+    if (count === 0) return alert('No se pudo borrar, revise RLS en Supabase.');
+  }
 
- allODS = allODS.filter(x => x.id !== o.id);
- currentSelectedId = null;
- window.currentSelectedId = null;
- renderSidebar(document.getElementById('search-input').value);
- document.getElementById('detail-view').innerHTML = `<div class="empty-selection"><h3>Orden Eliminada</h3></div>`;
- } catch (e) {
- alert('Error: ' + e.message);
- }
- };
+  allODS = allODS.filter(x => x.id !== o.id);
+  currentSelectedId = null;
+  window.currentSelectedId = null;
+  renderSidebar(document.getElementById('search-input').value);
+  document.getElementById('detail-view').innerHTML = `<div class="empty-selection"><h3>Orden Eliminada</h3></div>`;
+  } catch (e) {
+  alert('Error: ' + e.message);
+  }
+  };
 
  document.getElementById('btn-add-event').onclick = async function () {
  const btn = this;
@@ -3875,7 +3956,11 @@ if(hist[noteIdx]) {
  };
 
  // Run
- document.addEventListener('DOMContentLoaded', () => {
+ document.addEventListener('DOMContentLoaded', async () => {
+ await window.__mwDashboardReady;
+ applyAdminUI();
+ checkSupabaseSession();
+
  if (window.MacWaveAuditLog) {
  MacWaveAuditLog.init({
  supabaseClient,
@@ -3887,23 +3972,31 @@ if(hist[noteIdx]) {
  const viewParam = urlParams.get('view');
  const idParam = urlParams.get('id');
 
- loadData().then(() => {
+ const searchInput = document.getElementById('search-input');
+ if (searchInput) {
+ searchInput.addEventListener('input', (e) => renderSidebar(e.target.value));
+ }
+
+ await loadData();
  if (viewParam) {
  switchView(viewParam);
  if (idParam) {
  const target = allODS.find(o => o.folio === idParam || o.id === idParam);
  if (target) renderDetail(target);
  }
+ } else {
+ switchView('list');
  }
- });
 
  const lBtn = document.getElementById('logout-btn-side');
- if (lBtn) {
- lBtn.onclick = () => {
+ const mLogout = document.getElementById('m-nav-logout');
+ const doLogout = async () => {
+ try { await supabaseClient.auth.signOut(); } catch (_) { /* ignore */ }
  sessionStorage.clear();
  window.location.href = 'tecnicos.html';
  };
- }
+ if (lBtn) lBtn.onclick = doLogout;
+ if (mLogout) mLogout.onclick = doLogout;
 
  // Default date and time to today/now
  const now = new Date();
